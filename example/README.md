# Molecule Proxmox Example

This working example shows how to use one wrapper-script to share the proxmox-config across multiple roles without hardcoding them. 

Entrypoint: `molecule.sh`

Run example: `bash molecule.sh dummy converge`
